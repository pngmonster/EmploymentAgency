upstream django {
    server web:8000;
}

# HTTP — редирект на HTTPS (после получения сертификата раскомментировать)
server {
    listen 80;
    server_name _;

    # Для certbot
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Временно — пока нет SSL, отдаём напрямую
    # Когда получишь сертификат — замени этот блок на redirect:
    # return 301 https://$host$request_uri;

    location /static/ {
        alias /app/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location /media/ {
        alias /app/media/;
        expires 7d;
    }

    location / {
        proxy_pass         http://django;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_redirect     off;
        client_max_body_size 20M;
    }
}

# HTTPS — раскомментировать после получения сертификата
# server {
#     listen 443 ssl;
#     server_name yourdomain.com;
#
#     ssl_certificate     /etc/letsencrypt/live/yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/yourdomain.com/privkey.pem;
#
#     location /static/ {
#         alias /app/staticfiles/;
#         expires 30d;
#     }
#
#     location /media/ {
#         alias /app/media/;
#     }
#
#     location / {
#         proxy_pass         http://django;
#         proxy_set_header   Host              $host;
#         proxy_set_header   X-Real-IP         $remote_addr;
#         proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
#         proxy_set_header   X-Forwarded-Proto $scheme;
#         client_max_body_size 20M;
#     }
# }
